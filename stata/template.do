* template.do
* Example non-interactive Stata workflow skeleton

clear all
set more off

* Example paths (relative to repository root if run from root)
* import delimited using "data/raw/example.csv", clear
* save "data/processed/example.dta", replace

display "Stata template completed."
