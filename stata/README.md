# Stata (optional)

If parts of the workflow must be in Stata:
- keep `.do` files non-interactive
- write outputs to `data/processed/` or `data/derived/`
- record Stata version and package dependencies
- avoid manual point-and-click steps

Use `template.do` as a starting point.
